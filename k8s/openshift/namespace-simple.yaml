---
# Simple namespace for OpenShift backup system
# No special annotations or labels needed
apiVersion: v1
kind: Namespace
metadata:
  name: backup-system
  labels:
    app: cluster-backup
    platform: openshift
  annotations:
    description: "Simple namespace for OpenShift cluster backup system"